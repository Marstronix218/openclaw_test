# torch is installed separately from the CPU wheel index (see 40-model-serve.sh)
transformers>=4.45.0
accelerate>=0.34.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# Weave (Weights & Biases) LLM tracing for the OpenAI-compatible endpoint.
weave>=0.51.0
